- hosts: all
  gather_facts: no
  remote_user: ustudy
  tasks:
    - name: create cron job
      command: "crontab -u root -l | { cat; echo '*/5 * * * * /bin/bash /home/ustudy/scripts/auto-sc.sh'; } | crontab -u root -"
